---
interface Props {
    src: string;
    title: string;
}

const { title, src } = Astro.props;
---

<div {title} style={ `background: url(${src}) center center /cover` }></div>

<style>
    div {
        width: 100%;
        height: 10em;
        border-radius: 5px;
    }
</style>
